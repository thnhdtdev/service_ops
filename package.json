{
	"name": "service-ops",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev:frontend": "npm --prefix frontend run dev",
		"dev:backend": "go -C backend run ./cmd/api",
		"build:frontend": "npm --prefix frontend run build",
		"build:backend": "go -C backend build -o bin/api ./cmd/api",
		"lint:frontend": "npm --prefix frontend run lint",
		"format:frontend": "npm --prefix frontend run format",
		"format:check:frontend": "npm --prefix frontend run format:check",
		"test:backend": "go -C backend test ./..."
	},
	"engines": {
		"node": ">=20"
	}
}
